import RPi.GPIO as GPIO
import time

GPIO.setmode(GPIO.BOARD)
GPIO.setup(12,GPIO.IN)

i = 0

while  i != 100:
        i = i+1
        if GPI.input(12):
                print('LED in ON')
        else:
                print('LED IN OFF')

